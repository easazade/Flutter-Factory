class: User
table: users

fields:
  username: String
  password: String?, database
  profileImages: ProfileImages?, api
  preferences: List<String>?, api

indexes:
  username_idx:
    fields: username
